# АЖИЛЛАХ орчны багц (Vercel энэ файлыг уншина).
# Тест, хөгжүүлэлтийнх нь requirements-dev.txt дотор.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
fpdf2>=2.7
openpyxl>=3.1
python-multipart>=0.0.9
# Neon Postgres — psycopg 3. `[binary]` нь хөрвүүлэгчгүйгээр суудаг
# (Vercel-ийн build дээр libpq-dev байхгүй).
psycopg[binary]>=3.2
# Postgres-ийн схем (SQLite нь app/schema.py-ээр явна)
alembic>=1.13
